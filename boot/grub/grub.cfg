set timeout=0
set default=0

menuentry "CopperOS" {
    linux /boot/bzImage console=tty0 console=ttyS0,115200 init=/init //somehow i forgot to set the console in an operating system, that is just one big console
    initrd /boot/rootfs.cpio
    boot
}